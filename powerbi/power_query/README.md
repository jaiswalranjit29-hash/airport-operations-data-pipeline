# Power Query Scripts

Create a Power BI text parameter named `ProjectRoot`, for example:

```text
C:\Users\YourName\Documents\airport-operations-data-pipeline
```

Create blank queries and paste the corresponding `.pq` code. The small dimension files `dim_airline.csv` and `dim_airport.csv` can be imported directly through the standard CSV connector.
